{
  "scripts": {
    "open": "cypress open",
    "test": "cypress run",
    "cy:run": "cypress run --spec cypress/e2e/*.feature"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^24.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.8",
    "cypress": "^15.10.0"
  }
}
